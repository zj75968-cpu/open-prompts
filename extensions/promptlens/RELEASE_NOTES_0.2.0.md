# 灵感图镜 PromptLens 0.2.0

## 更新亮点

- 新增 `OpenAI Compatible` 接口支持
- 识图和生图支持分开选择服务商
- 不同服务商的 API Key、Base URL、模型名称独立记忆
- 优化对微博等防盗链站点的图片读取兼容性
- 修复扩展加载、配置切换和默认值回填问题

## 主要更新内容

### 1. 新增 OpenAI Compatible 接口支持

现在可以接入兼容 OpenAI API 格式的服务商，用于：

- 图片识别
- 图片生成

默认值：

- 识图默认模型：`gpt-5.5`
- 生图默认模型：`gpt-image-2`
- 默认 Base URL：`https://api.openai.com/v1`

### 2. 识图和生图支持分开选择服务商

识图和生图不再强制使用同一个服务商，可以自由组合使用，例如：

- 识图使用 `OpenAI Compatible`，生图使用 `Gemini`
- 识图使用 `Gemini`，生图使用 `OpenAI Compatible`

### 3. 不同服务商配置改为独立记忆

不同服务商的以下配置会分别保存：

- API Key
- Base URL
- 模型名称

切换服务商时不会再互相覆盖。

### 4. 优化图片读取稳定性

增强了网页图片读取方式，对微博等防盗链站点的兼容性更好。当前版本会根据页面情况自动选择更稳的取图方式。

### 5. 修复若干设置与加载问题

- 修复部分情况下 service worker 无法正常启动的问题
- 修复 provider 切换时配置状态串写的问题
- 修复默认值不自动回填的问题
- 修复旧配置迁移后 Base URL 被错误保留的问题

## 升级提示

- 如果你此前只使用 Gemini，升级后原有配置会尽量自动迁移。
- 如果你曾在早期预览版里切换过不同服务商，建议升级后重新检查一次识图和生图的 Base URL、模型名称与 API Key。

## 已知说明

- `OpenAI Compatible` 代表兼容 OpenAI API 请求格式的服务商或网关，不保证所有兼容平台都完全支持相同接口与模型。
- 不同账号、地区、套餐或兼容平台的模型可用性可能不同。
