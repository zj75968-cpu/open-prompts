# 灵感图镜 PromptLens 0.2.1

## 更新亮点

- 修复部分旧配置仍停留在代理模式，导致识图或生图调用失败的问题
- 修复 Gemini 自定义接口地址被错误重置的问题
- 优化 0.2.0 的配置迁移稳定性

## 主要修复内容

### 1. 修复旧代理配置保存不完整的问题

修复后，旧用户如果本地仍保留代理模式相关配置，重新保存设置时不会再因为字段丢失而导致调用异常。

涉及字段包括：

- `apiMode`
- `customProxyUrl`
- `customProxyToken`

### 2. 修复 Gemini 自定义 Base URL 误判问题

修复后，不会再因为地址以 `/v1` 结尾，就把用户自定义的 Gemini 网关误判成 OpenAI Compatible 地址并重置。

### 3. 继续兼容 0.2.0 的多服务商配置结构

`0.2.1` 保留了 `0.2.0` 引入的以下能力：

- `OpenAI Compatible` 接口支持
- 识图和生图分开选择服务商
- 不同服务商的 API Key、Base URL、模型名称独立记忆

## 升级提示

- 如果你已经使用 `0.2.0`，建议升级到 `0.2.1`
- 如果你配置过代理模式或自定义 Gemini 网关，这次升级尤其建议尽快同步
