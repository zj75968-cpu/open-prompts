# Privacy Policy

灵感图镜 PromptLens 当前采用“扩展客户端 + 服务器后台”架构。扩展端不保存 API Key、模型名称或模型 Base URL；这些敏感配置只应保存在你自己部署和管理的后端服务器中。

## 扩展本地保存的数据

扩展会使用 `chrome.storage.local` 在当前浏览器本地保存以下公共偏好：

- 后端 API 地址
- 是否打开面板后自动识别
- 是否显示生图功能
- 默认生图比例
- 默认出图张数

扩展端不会保存：

- API Key
- 模型名称
- 模型 Base URL
- 模型服务商私密配置

## 服务器端保存的数据

后端管理员后台会保存识图和生图所需配置，例如：

- 服务商类型
- API Key
- Base URL
- 模型名称
- 生图开关和额度类设置

本地开发默认保存到 `server/data/settings.json`。正式 VPS 当前使用你自部署的 `https://impro.n8nmydomain.com` 后端；真实密钥只应保存在服务器上。`server/data/settings.json` 已加入 `.gitignore`，不要提交真实密钥。

## 发送到后端和外部 API 的数据

当用户主动点击网页图片上的识别按钮时，扩展会读取该图片，并将图片数据、页面地址和图片 alt 文本发送到配置的后端 `/api/analyze`。

后端会根据管理员配置，把必要数据转发给识图模型服务商，用于生成提示词。

当用户主动点击「立刻生图」时，扩展会将当前提示词、负面提示词、生图比例和出图张数发送到后端 `/api/generate`。

后端会根据管理员配置，把必要数据转发给生图模型服务商，用于生成图片。

外部 API 对这些数据的处理方式，受对应服务商自己的服务条款和隐私政策约束。

## 取图兼容逻辑说明

为了提高在复杂网站上的识图成功率，扩展可能会按以下顺序尝试获取图片数据：

1. 直接读取页面内可访问的图片数据
2. 使用页面上下文请求图片资源
3. 在必要时对当前可见页面截图，并裁出用户当前点击的图片区域
4. 如果扩展端无法读取图片，则后端可能尝试通过图片 URL 拉取图片

这些操作都只发生在用户主动点击识图按钮之后。

## 项目作者不收集的数据

项目作者不会收集、存储或分析以下数据：

- 用户的 API Key
- 用户浏览的网页内容
- 用户识别的图片内容
- 用户生成或编辑的提示词
- 用户生成的图片结果

如果你把后端部署到自己的 VPS 或云服务器，上述数据会经过你自己的服务器。请根据实际部署环境配置 HTTPS、访问控制、日志保留和额度限制。

## 权限说明

- `storage`：用于保存扩展公共偏好。
- `tabs`：用于打开结果查看页，以及在必要时处理当前标签页截图兜底。
- `<all_urls>`：用于在网页图片上注入识别按钮，并在用户主动点击后读取对应图片。
- `https://impro.n8nmydomain.com/*`：用于请求你部署在 VPS 上的正式后端。
- `http://localhost:8787/*`、`http://127.0.0.1:8787/*`：仅用于本地开发后端。

扩展只会在用户主动触发识图或生图时调用后端 API。